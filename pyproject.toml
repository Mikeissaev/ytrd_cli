[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ytrd"
dynamic = ["version"]
description = "Утилита командной строки для скачивания видео с YouTube с автоматическим наложением закадрового перевода."
readme = "README.md"
requires-python = ">=3.8"
authors = [
  {name = "id-ex", email = "scr89shadow@gmail.com"},
]
dependencies = [
  "requests",
  "yt-dlp",
  "tqdm",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
ytrd = "ytrd.main:entry_point"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "ytrd.__version__"}